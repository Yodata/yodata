{
	"name": "{{name}}",
	"description": "{{description}}",
	"version": "0.0.1",
	"scripts": {
    "deploy": "npx deploy",
    "test": "npx jest --env node"
	},
	"devDependencies": {
		"@yodata/context-sdk": "^1.x"
	}
}
