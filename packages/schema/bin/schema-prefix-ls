#! /usr/bin/env node
const schema = require("commander");

schema
  .version("0.0.1")
  .name("schema prefix ls")
  .action(function() {
    console.log('todo:  list prefixes from the current schema')
  })
  .parse(process.argv);
