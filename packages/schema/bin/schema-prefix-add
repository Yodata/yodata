#! /usr/bin/env node

const command = require('commander')


command
  .name('schema prefix add')
  .description('adds a prefix to the current schema')
  .arguments('<namespace> [uri]')
  .action(function(namespace, uri) {

  })
  .parse(process.argv)
